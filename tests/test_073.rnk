affiche "Début test 073"
variable a = 73
affiche a
variable b = 74
affiche a + b
variable l = [73, 74, 75]
affiche l[1]
fonction f(x):
    affiche x * 75
f(74)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 73
affiche "Fin test 073"
