affiche "Début test 049"
variable a = 49
affiche a
variable b = 50
affiche a + b
variable l = [49, 50, 51]
affiche l[1]
fonction f(x):
    affiche x * 51
f(50)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 49
affiche "Fin test 049"
