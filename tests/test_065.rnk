affiche "Début test 065"
variable a = 65
affiche a
variable b = 66
affiche a + b
variable l = [65, 66, 67]
affiche l[1]
fonction f(x):
    affiche x * 67
f(66)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 65
affiche "Fin test 065"
