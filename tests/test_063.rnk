affiche "Début test 063"
variable a = 63
affiche a
variable b = 64
affiche a + b
variable l = [63, 64, 65]
affiche l[1]
fonction f(x):
    affiche x * 65
f(64)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 63
affiche "Fin test 063"
