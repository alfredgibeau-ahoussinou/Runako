affiche "Début test 059"
variable a = 59
affiche a
variable b = 60
affiche a + b
variable l = [59, 60, 61]
affiche l[1]
fonction f(x):
    affiche x * 61
f(60)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 59
affiche "Fin test 059"
