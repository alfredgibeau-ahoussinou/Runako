affiche "Début test 050"
variable a = 50
affiche a
variable b = 51
affiche a + b
variable l = [50, 51, 52]
affiche l[1]
fonction f(x):
    affiche x * 52
f(51)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 50
affiche "Fin test 050"
