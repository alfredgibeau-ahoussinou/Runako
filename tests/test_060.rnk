affiche "Début test 060"
variable a = 60
affiche a
variable b = 61
affiche a + b
variable l = [60, 61, 62]
affiche l[1]
fonction f(x):
    affiche x * 62
f(61)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 60
affiche "Fin test 060"
