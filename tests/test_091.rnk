affiche "Début test 091"
variable a = 91
affiche a
variable b = 92
affiche a + b
variable l = [91, 92, 93]
affiche l[1]
fonction f(x):
    affiche x * 93
f(92)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 91
affiche "Fin test 091"
