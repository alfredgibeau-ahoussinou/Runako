affiche "Début test 084"
variable a = 84
affiche a
variable b = 85
affiche a + b
variable l = [84, 85, 86]
affiche l[1]
fonction f(x):
    affiche x * 86
f(85)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 84
affiche "Fin test 084"
