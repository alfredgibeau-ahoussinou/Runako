affiche "Début test 020"
variable a = 20
affiche a
variable b = 21
affiche a + b
variable l = [20, 21, 22]
affiche l[1]
fonction f(x):
    affiche x * 22
f(21)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 20
affiche "Fin test 020"
