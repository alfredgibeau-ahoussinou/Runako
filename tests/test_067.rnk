affiche "Début test 067"
variable a = 67
affiche a
variable b = 68
affiche a + b
variable l = [67, 68, 69]
affiche l[1]
fonction f(x):
    affiche x * 69
f(68)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 67
affiche "Fin test 067"
