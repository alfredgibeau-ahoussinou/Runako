affiche "Début test 082"
variable a = 82
affiche a
variable b = 83
affiche a + b
variable l = [82, 83, 84]
affiche l[1]
fonction f(x):
    affiche x * 84
f(83)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 82
affiche "Fin test 082"
