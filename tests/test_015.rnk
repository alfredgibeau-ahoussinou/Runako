affiche "Début test 015"
variable a = 15
affiche a
variable b = 16
affiche a + b
variable l = [15, 16, 17]
affiche l[1]
fonction f(x):
    affiche x * 17
f(16)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 15
affiche "Fin test 015"
