affiche "Début test 100"
variable a = 100
affiche a
variable b = 101
affiche a + b
variable l = [100, 101, 102]
affiche l[1]
fonction f(x):
    affiche x * 102
f(101)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 100
affiche "Fin test 100"
