affiche "Début test 034"
variable a = 34
affiche a
variable b = 35
affiche a + b
variable l = [34, 35, 36]
affiche l[1]
fonction f(x):
    affiche x * 36
f(35)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 34
affiche "Fin test 034"
