affiche "Début test 039"
variable a = 39
affiche a
variable b = 40
affiche a + b
variable l = [39, 40, 41]
affiche l[1]
fonction f(x):
    affiche x * 41
f(40)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 39
affiche "Fin test 039"
