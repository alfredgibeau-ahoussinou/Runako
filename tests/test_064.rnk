affiche "Début test 064"
variable a = 64
affiche a
variable b = 65
affiche a + b
variable l = [64, 65, 66]
affiche l[1]
fonction f(x):
    affiche x * 66
f(65)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 64
affiche "Fin test 064"
