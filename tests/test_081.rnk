affiche "Début test 081"
variable a = 81
affiche a
variable b = 82
affiche a + b
variable l = [81, 82, 83]
affiche l[1]
fonction f(x):
    affiche x * 83
f(82)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 81
affiche "Fin test 081"
