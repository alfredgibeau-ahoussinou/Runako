affiche "Début test 041"
variable a = 41
affiche a
variable b = 42
affiche a + b
variable l = [41, 42, 43]
affiche l[1]
fonction f(x):
    affiche x * 43
f(42)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 41
affiche "Fin test 041"
