affiche "Début test 042"
variable a = 42
affiche a
variable b = 43
affiche a + b
variable l = [42, 43, 44]
affiche l[1]
fonction f(x):
    affiche x * 44
f(43)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 42
affiche "Fin test 042"
