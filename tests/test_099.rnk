affiche "Début test 099"
variable a = 99
affiche a
variable b = 100
affiche a + b
variable l = [99, 100, 101]
affiche l[1]
fonction f(x):
    affiche x * 101
f(100)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 99
affiche "Fin test 099"
