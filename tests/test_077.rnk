affiche "Début test 077"
variable a = 77
affiche a
variable b = 78
affiche a + b
variable l = [77, 78, 79]
affiche l[1]
fonction f(x):
    affiche x * 79
f(78)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 77
affiche "Fin test 077"
