affiche "Début test 094"
variable a = 94
affiche a
variable b = 95
affiche a + b
variable l = [94, 95, 96]
affiche l[1]
fonction f(x):
    affiche x * 96
f(95)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 94
affiche "Fin test 094"
