affiche "Début test 075"
variable a = 75
affiche a
variable b = 76
affiche a + b
variable l = [75, 76, 77]
affiche l[1]
fonction f(x):
    affiche x * 77
f(76)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 75
affiche "Fin test 075"
