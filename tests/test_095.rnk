affiche "Début test 095"
variable a = 95
affiche a
variable b = 96
affiche a + b
variable l = [95, 96, 97]
affiche l[1]
fonction f(x):
    affiche x * 97
f(96)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 95
affiche "Fin test 095"
