affiche "Début test 022"
variable a = 22
affiche a
variable b = 23
affiche a + b
variable l = [22, 23, 24]
affiche l[1]
fonction f(x):
    affiche x * 24
f(23)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 22
affiche "Fin test 022"
