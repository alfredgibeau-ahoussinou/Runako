affiche "Début test 061"
variable a = 61
affiche a
variable b = 62
affiche a + b
variable l = [61, 62, 63]
affiche l[1]
fonction f(x):
    affiche x * 63
f(62)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 61
affiche "Fin test 061"
