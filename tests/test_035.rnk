affiche "Début test 035"
variable a = 35
affiche a
variable b = 36
affiche a + b
variable l = [35, 36, 37]
affiche l[1]
fonction f(x):
    affiche x * 37
f(36)
si a < b:
    affiche "ok"
si a > b:
    affiche "fail"
# Test erreur variable non définie
si z > 0:
    affiche z
# Test boucle
pour i de 1 à 3:
    affiche i * 35
affiche "Fin test 035"
